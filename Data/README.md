# Original data backup

This directory stores the original FASTA and metadata data as preserved source files.

Do not modify these files directly. When running the pipeline, use the copies in `01_sample_preparation/input/`.
